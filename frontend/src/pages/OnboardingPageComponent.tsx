import type { ComponentType } from "react";

// @ts-ignore Legacy JSX page retained as-is for now.
import OnboardingPage from "./OnboardingPage.jsx";

export default OnboardingPage as ComponentType<{ embedded?: boolean }>;
