export function EsgMonitoringPage() {
  return (
    <div className="page-shell">
      <section className="page-header px-8 py-8">
        <div className="max-w-3xl">
          <p className="eyebrow text-sm">Continuous Monitoring</p>
          <h1 className="mt-3 text-3xl font-semibold tracking-tight text-[var(--text)] sm:text-4xl">
            Continuous Monitoring workspace
          </h1>
          <p className="mt-4 text-sm leading-6 text-[var(--text-secondary)] sm:text-base">
            This page is intentionally cleared so the next implementation can be designed around the
            right monitoring workflow, signals, alerts, and client demo story.
          </p>
        </div>
      </section>

      <section className="visual-card p-8">
        <div className="max-w-3xl">
          <h2 className="visual-title">Ready for redesign</h2>
          <p className="visual-description mt-3">
            The previous ESG monitoring dashboard content has been removed from the frontend. We can
            now define what Continuous Monitoring should include before rebuilding this module.
          </p>
        </div>
      </section>
    </div>
  );
}
